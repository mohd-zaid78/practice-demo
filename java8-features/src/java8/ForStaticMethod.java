package java8;

public interface ForStaticMethod {
	static void demo(){
		System.out.println("this is the static method ");
		System.out.println("we can declare static concrete method in interface if and only if the jdk version is 8");
	}
}
